import React from 'react'

const Languagedetails = ({langList}) => {
  return (
  
      <i className={langList.icon +" icons"} style={{color:langList.color}}></i>
  
  )
}

export default Languagedetails
